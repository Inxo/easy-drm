# AES-256 key (hex) — generate with: openssl rand -hex 32
AES_KEY=your_64_hex_chars_aes_key_here

# HMAC secret for player tokens — generate with: openssl rand -hex 32
TOKEN_SECRET=your_token_signing_secret_here
